[build]
buildCommand = "bun install && bunx prisma generate && bun run build"

[deploy]
startCommand = "bun run start"
healthcheckPath = "/"
preDeployCommand = "bunx prisma db push --config prisma/prisma.config.ts --accept-data-loss"
