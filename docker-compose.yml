services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: tydei
      POSTGRES_PASSWORD: tydei_dev_password
      POSTGRES_DB: tydei
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
